{
  "name": "230629_css-buttno-recipes",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "compile": "rimraf docs/*.css docs/**/*.css && sass src/styles:docs --no-source-map && stylelint \"**/*.css\" --fix",
    "format": "stylelint \"**/*.scss\" --fix"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "stylelint": "15.7.0",
    "stylelint-config-recess-order": "4.2.0",
    "stylelint-config-standard": "33.0.0"
  },
  "dependencies": {
    "sass": "1.63.4",
    "stylelint-config-standard-scss": "9.0.0"
  }
}
